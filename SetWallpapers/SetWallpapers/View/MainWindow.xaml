<Window x:Class="SetWallpapers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SetWallpapers.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">



    <Window.DataContext>
        <local:MainWindowViewModel></local:MainWindowViewModel>
    </Window.DataContext>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ListBox Margin="5" Name="ListBoxCategories" ItemsSource="{Binding Path = Categories}">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <CheckBox Content="{Binding}"></CheckBox>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

            <ListBox Margin="5" Grid.Column="1" Name="ListBoxResolutions" ItemsSource="{Binding Path = Resolutions}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <RadioButton Content="{Binding}"></RadioButton>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>

        <Grid Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <WrapPanel VerticalAlignment="Center" Margin="5,0" Orientation="Horizontal">

                <TextBlock VerticalAlignment="Center" Margin="0,0,10,0"  >Set interval:</TextBlock>

                <ComboBox Grid.Row="1" Width="100" Height="25">
                    <ComboBoxItem>
                        5 min
                    </ComboBoxItem>
                    <ComboBoxItem>
                        30 min
                    </ComboBoxItem>
                    <ComboBoxItem>
                        1 hour
                    </ComboBoxItem>
                    <ComboBoxItem>
                        1 day
                    </ComboBoxItem>
                </ComboBox>
            </WrapPanel>

            <Button Grid.Row="1" Height="25" Width="100">Save</Button>

        </Grid>




        




    </Grid>
</Window>
